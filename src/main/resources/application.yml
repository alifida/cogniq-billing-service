server:
  port: 8084

spring:
  application:
    name: cogniq-billing-service
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/cogniq_billing
    username: postgres
    password: postgres123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# Swagger / OpenAPI (accessible via Gateway at /api/billing/v3/api-docs)
springdoc:
  api-docs:
    path: /api/billing/v3/api-docs
  swagger-ui:
    path: /api/billing/swagger-ui.html

# Actuator & Prometheus
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
  health:
    db:
      enabled: true
  prometheus:
    metrics:
      export:
        enabled: true

cogniq:
  jwt:
    secret: ""

# Resilience4j
resilience4j:
  circuitbreaker:
    instances:
      dbCircuitBreaker:
        registerHealthIndicator: true
        slidingWindowSize: 10
        failureRateThreshold: 50
        waitDurationInOpenState: 10s

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

logging:
  level:
    root: INFO
    "[com.cognitivequantum]": DEBUG
    "[org.hibernate.SQL]": DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
